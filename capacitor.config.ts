import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'e-bill-tracker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
